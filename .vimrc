" set compatibility to vim only, not vi
set nocompatible

" show line numbers
set number

" status bar
set laststatus=2

" enable wrapping
set wrap

" encoding
set encoding=utf-8

" colors
colorscheme slate

" 2 space tabs
set expandtab
set tabstop=2
set softtabstop=2
set shiftwidth=2
